```bash
Repo này để tạo database phục vụ cho việc nghiên cứu khoa học, mục đích chính đáng và đóng góp lớn cho xã hội chủ nghĩa
#Chạy lệnh sau để cài thư viện
python -m venv .venv
source .venv/bin/activate  # Hoặc .\.venv\Scripts\activate trên Windows
pip install -r requirements.txt
```
