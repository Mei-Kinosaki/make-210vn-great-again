# make-210vn-great-again
unemployment behaviors
# chạy lệnh sau để cài thư viện cần
python -m venv .venv
source .venv/bin/activate  # Hoặc .\.venv\Scripts\activate trên Windows
pip install -r requirements.txt
